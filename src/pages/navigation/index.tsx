import { useMemo } from 'react'
import NavCard, { NavCardProps } from './components/navCard'

const Navigation = () => {
  return <div className="navigation-container">sss</div>
}

export default Navigation
